import React,{useEffect,useState} from 'react';import './HomartPrinterTonerLevel.css';const HomartPrinterTonerLevel=()=>{const[data,setData]=useState([]);useEffect(()=>{const fetchData=async()=>{const response=await fetch('http://localhost:3030/api/v1/homart/printer/tonerlevel');const result=await response.json();setData(result);};fetchData();},[]);return(<div className="table-container"><table className="printer-table"><thead><tr><th></th>{data.map((printer,i)=><th key={i}>{printer.name}</th>)}</tr></thead><tbody>{data.length>0&&Object.keys(data[0]).map((property,i)=>property!=='name'&&(<tr key={i}><th>{property}</th>{data.map((printer,j)=><td key={j}>{printer[property]}</td>)}</tr>))}</tbody></table></div>);};export default HomartPrinterTonerLevel;
